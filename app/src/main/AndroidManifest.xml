<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeSpotter"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.SplashActivity"
            android:theme="@style/Theme.App.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.PerfilActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
        <activity android:name=".ui.activities.RegisterActivity" />
        <activity android:name=".ui.activities.ModifyProfileActivity" />
        <activity android:name=".ui.activities.ViviendaAddActivity" />
        <activity android:name=".ui.activities.ViviendaActivity" />
        <activity android:name=".ui.activities.MensajeActivity" />
        <activity android:name=".ui.activities.FavoritesActivity" />
        <activity android:name=".ui.activities.ChatListActivity" />
        <activity android:name=".ui.activities.ViviendaDetailActivity" />
        <activity android:name=".ui.activities.MainActivity" />

    </application>

</manifest>